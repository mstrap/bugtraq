# An example content of .gitbugtraq to put in the repository root directory.
# It could instead be added as an additional section to $GIT_DIR/config.
# (note that '\' need to be escaped).

#Sample repository: https://codeberg.org/daniels_199/gitbugtraqdemo.git

[bugtraq]
  url = https://codeberg.org/<user>/<project>/issues/%BUGID%
  loglinkregex = "#\\d+"
  logregex = \\d+